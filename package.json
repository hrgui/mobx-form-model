{
  "name": "@hrgui/mobx-form-model",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "jest",
    "docs": "typedoc --out docs/ --theme markdown --exclude '**/*.spec.+(ts|tsx)' --readme none src/",
    "build": "tsc"
  },
  "devDependencies": {
    "lerna": "^2.9.0"
  }
}
