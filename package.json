{
  "name": "mobx-form-model",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prerelease": "lerna run prePublishOnly",
    "release": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^2.9.0"
  }
}
